import { styled } from "@mui/system";

export const RadioActiveNumber = styled('span')(() => ({
    color: "white",
}));

export const RadioInactiveNumber = styled('span')(() => ({
    color: "#B6C7CB"
}));

