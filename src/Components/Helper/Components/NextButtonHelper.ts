import { styled } from "@mui/system";

const NextButtonHelper = styled('button')(({ theme }) => ({
    all:"unset",
    cursor: "pointer",
    position: "absolute",
    right: "-5em",
    top: "50%"
}));
export default NextButtonHelper;