import { styled } from "@mui/system";
import { Box } from "@mui/material";

const FormBoxButton = styled(Box)({
    display: "flex",
    justifyContent: "flex-end",
    gap: "1em",
    margin: "1em",
});
export default FormBoxButton;
