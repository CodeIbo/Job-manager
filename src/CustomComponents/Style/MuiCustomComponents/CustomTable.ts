
import { Table } from '@mui/material'
import styled from '@emotion/styled'
const CustomT = styled(Table)({
    width: "90vw",
    tableLayout: "fixed",
    whiteSpace: 'normal',
    wordWrap: 'break-word',
})

export default CustomT