import Alert from '@mui/material/Alert';
import { styled } from "@mui/material/styles";

const AlertFrom = styled(Alert) ({
    position: 'absolute',
    top:'0',
    margin:'0 auto'

})

export default AlertFrom
